name: Clone Stats

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - uses: oficiallyAkshay/clonometer@main
        with:
          token: ${{ secrets.TRAFFIC_TOKEN }}